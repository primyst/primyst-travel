import Image from "next/image";
import { ArrowRight, Calendar, Clock } from "lucide-react";
import type { Package } from "./data";
import { COMPANY_NAME, openWhatsApp } from "./data";
export default function PackageCard({pkg}:{pkg:Package}){return <article className="group overflow-hidden rounded-3xl border border-gray-100 bg-white shadow-sm transition duration-300 hover:-translate-y-1 hover:shadow-2xl"><div className="relative h-60 overflow-hidden"><Image src={pkg.image} alt={pkg.title} fill className="object-cover transition duration-700 group-hover:scale-105" sizes="(max-width:640px) 100vw,(max-width:1024px) 50vw,33vw"/><div className="absolute inset-0 bg-gradient-to-t from-black/75 via-black/10 to-transparent"/><div className="absolute left-4 top-4 rounded-full bg-white/90 px-3 py-1.5 text-xl shadow-lg">{pkg.flag}</div><div className="absolute bottom-4 left-5 text-white"><p className="text-2xl font-black">{pkg.title}</p><p className="text-sm text-white/80">{pkg.location}</p></div></div><div className="p-6"><div className="mb-5 flex flex-wrap gap-x-5 gap-y-2 text-sm text-gray-500"><span className="flex items-center gap-1.5"><Calendar size={14}/>{pkg.dates}</span><span className="flex items-center gap-1.5"><Clock size={14}/>{pkg.duration}</span></div><p className="mb-3 text-[11px] font-bold uppercase tracking-[0.18em] text-[#0D9488]">Included in the trip</p><div className="mb-6 flex flex-wrap gap-2">{pkg.included.map(({icon:Icon,label})=><span key={label} className="flex items-center gap-1.5 rounded-full border border-gray-100 bg-gray-50 px-3 py-1.5 text-xs text-gray-600"><Icon size={12} className="text-[#F97316]"/>{label}</span>)}</div><div className="flex items-end justify-between gap-3 border-t border-gray-100 pt-5"><div><p className="text-xs text-gray-400">From / person</p><p className="text-2xl font-black text-[#0D9488]">{pkg.price}</p></div><button onClick={()=>openWhatsApp(`Hi ${COMPANY_NAME}! I'm interested in the *${pkg.title}* package (${pkg.dates}, ${pkg.price} per person).`)} className="flex items-center gap-1.5 rounded-xl bg-[#0D9488] px-4 py-3 text-sm font-bold text-white"><span>I want this</span><ArrowRight size={15}/></button></div></div></article>}
