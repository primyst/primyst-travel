"use client";
import { useState } from "react";
import Link from "next/link";
import { Menu, X, Send } from "lucide-react";
import TextLogo from "./TextLogo";
import { COMPANY_NAME, NAV_LINKS, openWhatsApp } from "./data";
export default function Navbar(){const [open,setOpen]=useState(false);return <><header className="fixed inset-x-0 top-0 z-50 border-b border-white/60 bg-white/90 backdrop-blur-xl"><div className="mx-auto flex max-w-6xl items-center justify-between px-4 py-3 sm:px-6"><Link href="/"><TextLogo/></Link><nav className="hidden items-center gap-1 md:flex">{NAV_LINKS.map(l=><Link key={l.href} href={l.href} className="rounded-full px-4 py-2 text-sm text-gray-600 transition hover:bg-gray-50 hover:text-[#0D9488]">{l.label}</Link>)}</nav><div className="flex items-center gap-3"><button onClick={()=>openWhatsApp(`Hi ${COMPANY_NAME}, I'd like to talk to a travel expert.`)} className="hidden items-center gap-2 rounded-full bg-[#0D9488] px-5 py-2.5 text-sm font-semibold text-white sm:flex"><Send size={14}/> Chat on WhatsApp</button><button onClick={()=>setOpen(!open)} className="p-2 text-[#0D9488] md:hidden" aria-label="Open menu">{open?<X/>:<Menu/>}</button></div></div></header>{open&&<div className="fixed inset-x-0 top-[61px] z-40 border-b border-gray-100 bg-white p-4 shadow-xl md:hidden"><nav className="flex flex-col gap-1">{NAV_LINKS.map(l=><Link key={l.href} href={l.href} onClick={()=>setOpen(false)} className="rounded-xl px-4 py-3 font-medium text-gray-700">{l.label}</Link>)}<button onClick={()=>{openWhatsApp(`Hi ${COMPANY_NAME}, I'd like to talk to a travel expert.`);setOpen(false)}} className="mt-2 flex items-center justify-center gap-2 rounded-xl bg-[#0D9488] py-3 font-semibold text-white"><Send size={16}/> Chat on WhatsApp</button></nav></div>}</>}
